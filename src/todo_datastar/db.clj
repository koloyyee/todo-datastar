(ns todo-datastar.db)


;; Dummy 
;; Fake items data
(def item1 {:id "item-1" :title "buy carrot" :description "get some vege" :done? false})

(def item2 {:id "item-2" :title "buy onion" :description "prepare for pickle onion" :done? false})

;;(def items (atom [item1 item2]))
